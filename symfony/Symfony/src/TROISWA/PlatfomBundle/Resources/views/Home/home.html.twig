{% extends "TROISWAPlatfomBundle::layout.html.twig" %}

{% block page_title %}
    <h1>Article</h1>
{% endblock %}

{% block troiswaplatfom_body %}

    <h1>Recherche développeur Symfony3</h1>

    <p><em>Par {{tab.author}}, le {{tab.date| date('d/m/Y')}}</em></p>

    <p>{{ tab.content }} </p>

    <p>
    <a href="{{ path('troiswa_platfom_advert', {'id': 0, name: 'Alan'}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-chevron-left"></i>
      Retour à la liste
    </a>
    <a href="{{ path('troiswa_platfom_edit', {'id': 20}) }}" class="btn btn-default">
      <i class="glyphicon glyphicon-edit"></i>
      Modifier l'annonce
    </a>
    <a href="{{ path('troiswa_platfom_delete', {'id': 20}) }}" class="btn btn-danger">
      <i class="glyphicon glyphicon-trash"></i>
      Supprimer l'annonce
    </a>
  </p>


{% endblock %}
